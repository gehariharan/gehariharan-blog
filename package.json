{
	"name": "hariragu-microsoft-fluffy-giggle",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:posts": "node scripts/build-posts.mjs",
		"new:post": "node scripts/new-post.mjs",
		"deploy": "node scripts/build-posts.mjs && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-plugin": "^1.0.0",
		"typescript": "^5.5.2",
		"vitest": "~4.1.0",
		"wrangler": "^4"
	}
}